# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: enterprise-cms-wagtail
metadata:
  template: enterprise-cms-wagtail@0.0.1-beta
services:
  wagtail-cms:
    project: ./cms/enterprise-cms
    language: python
    host: appservice
    docker:
      path: ./Dockerfile
      context: ./
infra:
  provider: bicep
